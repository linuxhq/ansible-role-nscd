---
- name: Ensure that the nscd package is installed
  tags: nscd
  yum:
    name: nscd
    state: present
    update_cache: yes
  register: nscd_yum

- block:
    - name: Applying the nscd.conf configuration
      template:
        src: nscd.conf.j2
        dest: /etc/nscd.conf
        owner: root
        group: root
        mode: 0644
      notify: restart nscd

    - name: Enable and start the nscd service
      service:
        enabled: yes
        name: nscd
        state: started
  tags: nscd
  when: nscd_yum|success
...
